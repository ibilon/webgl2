package js.webgl2.constants;

/**
Helper type over GLenum to limit functions to their valid constants when possible.

Represent the following constants:

* `RenderingContext2.OBJECT_TYPE`
* `RenderingContext2.SYNC_CONDITION`
* `RenderingContext2.SYNC_STATUS`
* `RenderingContext2.SYNC_FLAGS`
**/
abstract SyncParameterEnum (GLenum)
{
}
