package js.webgl2.constants;

/**
Helper type over GLenum to limit functions to their valid constants when possible.

Represent the following constants:

* `RenderingContext.STATIC_DRAW`
* `RenderingContext.STREAM_DRAW`
* `RenderingContext.DYNAMIC_DRAW`
* `RenderingContext2.STREAM_READ`
* `RenderingContext2.STREAM_COPY`
* `RenderingContext2.STATIC_READ`
* `RenderingContext2.STATIC_COPY`
* `RenderingContext2.DYNAMIC_READ`
* `RenderingContext2.DYNAMIC_COPY`
**/
abstract BufferUsageEnum (GLenum)
{
}
