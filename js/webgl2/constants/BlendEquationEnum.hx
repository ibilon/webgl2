package js.webgl2.constants;

/**
Helper type over GLenum to limit functions to their valid constants when possible.

Represent the following constants:

* `RenderingContext.FUNC_ADD`
* `RenderingContext.FUNC_SUBTRACT`
* `RenderingContext.FUNC_REVERSE_SUBTRACT`
* `RenderingContext2.MIN`
* `RenderingContext2.MAX`
**/
abstract BlendEquationEnum (GLenum)
{
}
